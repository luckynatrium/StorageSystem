<table>
<tr>
  <th>Name</th>
  <th>Quantity</th>
  <th>Last delivery date</th>
</tr>

<% @stocks.each do |stock| %>                    
    <tr>
      <td><%= stock.name %></td>
      <td><%= stock.quantity %></td> 
      <td><%= stock.date.strftime("%d/%m/%Y") %></td>
    </tr>
  <% end %>
</table>